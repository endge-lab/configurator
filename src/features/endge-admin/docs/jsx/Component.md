# Component

`Component` в DSL/JSX используется для встраивания дочернего доменного компонента в текущий runtime-контекст.

Типовые сценарии:
- композиция экранов из вложенных блоков;
- переиспользование уже созданных компонентов;
- проброс `scope` и runtime-данных в дочерний рендер.
