# Endge.debug

Собирает отладочную трассировку (спаны) выполнения операций. Позволяет записывать и читать цепочки вызовов, замерять время и строить дерево спанов. Используется для профилирования и отладки в админке и при разработке.
