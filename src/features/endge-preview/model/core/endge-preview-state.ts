import { EndgePreviewSession } from '@/features/endge-preview/model/core/endge-preview-session'

/** Cycle-free shared state used by the preview controller and its UI federation. */
export const endgePreviewSession = new EndgePreviewSession()
