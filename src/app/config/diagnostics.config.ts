import type { ConfiguratorDiagnosticsConfig } from '@/app/domain/types/configurator-diagnostics.type'

export const CONFIGURATOR_DIAGNOSTICS_CONFIG: ConfiguratorDiagnosticsConfig = {
  errorWindowMs: 2000,
  errorStormLimit: 8,
}
